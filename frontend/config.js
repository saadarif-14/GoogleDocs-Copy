// Runtime frontend configuration. Set BACKEND_URL to your backend endpoint.
// When deploying to Vercel set this file (or replace the value at build time).
window.BACKEND_URL = window.BACKEND_URL || '' // e.g. 'https://your-backend.example.com'
